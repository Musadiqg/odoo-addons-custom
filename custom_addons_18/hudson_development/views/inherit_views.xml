<odoo>
    <data>

         <record id="view_payment_form_asset_inherit_hudson" model="ir.ui.view">
        <field name="name">account.payment.form.hudson</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='journal_id']" position="before">
                <field name="label_ref" invisible="0"/>
                <field name="is_commercial" invisible="1"/>
            </xpath>
        </field>
        </record>

        <record id="view_account_form_asset_inherit_hudson" model="ir.ui.view">
        <field name="name">account.account.form.hudson</field>
        <field name="model">account.account</field>
        <field name="inherit_id" ref="account.view_account_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='allowed_journal_ids']" position="after">
                <field name="is_wht_account"/>
                <field name="is_sti_account" invisible="0"/>
                <field name="is_srb_account" invisible="0"/>
            </xpath>
        </field>
        </record>

          <record id="view_mrp_form_asset_inherit_hudson" model="ir.ui.view">
        <field name="name">mrp.production.form.hudson</field>
        <field name="model">mrp.production</field>
        <field name="inherit_id" ref="mrp.mrp_production_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id']" position="after">
                <field name="x_product_price" invisible="1"/>
            </xpath>
        </field>
        </record>

        <record id="view_account_payment_search_inherit_account_batch_payment_custom" model="ir.ui.view">
            <field name="name">account.payment.search.inherit.account_wht_payment</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_search" />
            <field name="arch" type="xml">

                <!-- Filter on WHT payments -->
                <filter name="outbound_filter" position="after">
                    <filter string="WHT Payments"
                            name="wht_payments_filter"
                            domain="[('x_studio_with_holding_tax.line_ids.account_id.is_wht_account', '=', True),('partner_id.x_TaxReportable','=','Yes')]"/>
                </filter>

            </field>
        </record>

          <record id="view_account_payment_search_inherit_account_move_jvs_srb" model="ir.ui.view">
            <field name="name">account.move.filter.hudson</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_account_move_filter" />
            <field name="arch" type="xml">

                <filter name="posted" position="after">
                    <filter string="SRB 100 % Unregistered"
                            name="srb_jvs_filter"
                            domain="[('line_ids.account_id.is_srb_account', '=', True)]"/>
                </filter>

            </field>
        </record>

         <record id="view_account_account_move_bill_hudson" model="ir.ui.view">
            <field name="name">account.move.bill.hudson</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form" />
            <field name="arch" type="xml">

                <xpath expr="//field[@name='ref']" position="after">
                    <field name="bill" invisible="is_srb != True"/>
                    <field name="is_srb" invisible="1"/>
                </xpath>

            </field>
        </record>

        <record id="sale.view_sales_order_filter" model="ir.ui.view">
            <field name="name">sale.order.list.select</field>
            <field name="model">sale.order</field>
            <field name="priority" eval="15"/>
            <field name="arch" type="xml">
                <search string="Search Sales Order">
                    <field name="name" string="Order" filter_domain="['|', '|', ('name', 'ilike', self), ('client_order_ref', 'ilike', self), ('partner_id', 'child_of', self)]"/>
                    <field name="partner_id" operator="child_of"/>
                    <field name="user_id"/>
                    <field name="team_id" string="Sales Team"/>
                    <field name="order_line" string="Product" filter_domain="[('order_line.product_id', 'ilike', self)]"/>
                    <!-- <field name="analytic_account_id" groups="analytic.group_analytic_accounting"/> -->
                    <field name="order_line" string="Analytic Tag" filter_domain="[('order_line.analytic_tag_ids.name','ilike', self)]"/>
                    <!-- We only allow to search on the following sale order line fields (product, name) because the other fields, such as price, quantity, ...
                        will not be searched as often, and if they need to be searched it's usually in the context of products
                        and then they can be searched from the page listing the sale order lines related to a product (from the product itself).
                    -->
                    <filter string="My Orders" domain="[('user_id', '=', uid)]" name="my_sale_orders_filter"/>
                    <filter invisible="1" string="Late Activities" name="activities_overdue"
                        domain="[('my_activity_date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"
                        help="Show all records which has next action date is before today"/>
                    <filter invisible="1" string="Today Activities" name="activities_today"
                        domain="[('my_activity_date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                    <filter invisible="1" string="Future Activities" name="activities_upcoming_all"
                        domain="[('my_activity_date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))]"/>
                    <group expand="0" string="Group By">
                        <filter string="Salesperson" name="salesperson" domain="[]" context="{'group_by': 'user_id'}"/>
                        <filter name="customer" string="Customer" domain="[]" context="{'group_by': 'partner_id'}"/>
                        <filter string="Order Date" name="order_month" domain="[]" context="{'group_by': 'date_order'}"/>
                    </group>
               </search>
            </field>
        </record>

        <record id="view_account_payment_register_for_commercial_import" model="ir.ui.view">
            <field name="name">view.account.payment.register.form.inherit.commercial</field>
            <field name="model">account.payment.register</field>
            <field name="inherit_id" ref="account.view_account_payment_register_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='communication']" position="after">
                        <field name="is_commercial"/>
                </xpath>
            </field>
        </record>

         <record id="view_crm_partner_exemption_date" model="ir.ui.view">
            <field name="name">res.partner.exemption_date</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='property_account_position_id']" position="before">
                    <field name="exemption_date"/>
                </xpath>
            </field>
         </record>


    </data>
</odoo>